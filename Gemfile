# frozen_string_literal: true

# -*-ruby -*-
source "https://rubygems.org"
gemspec

group :test do
  # Optional dependency, we do want to test with it
  gem "nokogiri"
end

platforms :rbx do
  gem "racc"
end
